apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: my-first-pod
spec:
  containers:
    - name: kubenginx
      image: stacksimplify/kubenginx:1.0.0
      ports:
        - containerPort: 80
