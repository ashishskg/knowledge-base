@RestController Annotation Composition

Answer
@RestController is composed of 2 annotations:
- @Controller
- @ResponseBody

Explanation
It is a convenience annotation that marks the class as a Spring MVC controller
and writes method return values directly to the HTTP response body (usually JSON).
